{
  "name": "df-memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun test --timeout=60000",
    "typecheck": "bun run tsc --noEmit",
    "memory": "bun run packages/cli/src/main.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}