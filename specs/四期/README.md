# 四期 Specs — 补充期（访问面）

> **定位**：**补充期**——不阻塞五/六/七期；未获明确要求前不实现。  
> **前提**：建议三期（P3.1–P3.3）已完成；**实现默认排在七期主线之后**（或用户点名再做）。  
> **原则**：agent 访问面绑定已稳定的检索/蒸馏/会话编译契约。  
> **本期产品承诺**：MCP + REST 子集 + 至少一个 agent 适配器；agent **不**把直接读仓文件当主 API。P6.5 的 CLI hook 换成本期 MCP，触发表不变。

## 实现顺序

| 顺序 | Spec | 文件 | 原 ID | 依赖 |
|---|---|---|---|---|
| 1 | P4.1 MCP/REST/适配器 | [`P4.1-mcp-rest-adapters.md`](P4.1-mcp-rest-adapters.md) | P2.3 | M2, P2.1a；建议 P2.2、P3.3 |

## 新增包（建议）

```
packages/mcp/
packages/adapters/claude-code/
packages/server/          # REST 可选与 cli 同进程
```

## 完成标志

P4.1 DoD 勾选后，访问面交付完成；可开正式版版本号规划。架构侧见 [`../五期/`](../五期/)；会话摄入见 [`../六期/`](../六期/)；LLM 真路径见 [`../七期/`](../七期/)（优先于本期）。
