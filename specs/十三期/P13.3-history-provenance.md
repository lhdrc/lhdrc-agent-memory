# Spec P13.3 — History 底层 + note 正排

| 字段 | 值 |
|---|---|
| ID | P13.3 |
| 状态 | done |
| 依赖 | P6.3、P7.3、P8.1 |
| 对应架构 | 08 D6；TODO **#53** |
| 仓库 | 本仓 `packages/core` |

> `history` 保原始不改，不进 `pages/chunks` 索引；`note` 正排指向 `history` 按需回跳。

## 1. 目标

1. 复用 `inbox/session.ts:98 archiveSession` 落全量 `messages.jsonl`。  
2. `note` 侧车 `history_index.jsonl` + 可选 frontmatter `provenance:{session_id, turns:[1,2], history_ref:"inbox/sessions/<id>/messages.jsonl#turn2-3"}`，存 `turn_index` 非 `byteOffset`。  
3. `memory read <path> --with-history` / `memory history read --session <id> --turn 2` 按需回跳。  
4. 检索仍只走 `pages/chunks`。

## 2. 非目标

- 把 `transcript` 整篇当 `L0`
- `fts_*/bigrams` 吃 `history`
- `content_hash` 吃 `provenance`

## 3. 技术方案（对标 `inbox/session.ts:98 archiveSession` 落 `messages.jsonl` + `142 messages.jsonl` 转 `Turn{role,text,at}` / `compile/session.ts:288 prefetch + LLM` 抽 `note` / `write/capture.ts:86 captureWrite` / `write/validator.ts:162` frontmatter 白名单 / `node/read.ts:24 readNode L0/L1/L2` / `retrieve/hybrid.ts:464 hybridQuery` + `retrieve/query-triggers.ts shouldQueryMemory`）

| 项 | 做法 | 文件 |
|---|---|---|
| 侧车 | `brains/{id}/history_index.jsonl` 每行 `{md_path, session_id, turn_index:[1,2], history_ref:"inbox/sessions/<id>/messages.jsonl#turn2-3"}`，存 `turn_index` 非 `byteOffset`（`messages.jsonl` append 稳定） | `inbox/session.ts` + `compile/session.ts` |
| 写 | `captureWrite` 后同事务 `appendFile history_index`，`provenance` 仅侧车，不进 `content-hash.ts:49 pickSemanticFrontmatter` 白名单（`content_hash` 不抖），`index/sync.ts:57 indexBodyText` 不吃 `history` | `write/capture.ts:86` + `write/validator.ts` + `write/queue.ts` |
| 读 | `memory read <path> --with-history` 合并 `raw + history slice`；`memory history read --session <id> --turn 2` 直读 `messages.jsonl` `sed -n`，`--explain` 透 `provenance` 供 `codex read_path.md:33 Quick pass` | `node/read.ts:24` + `cli/commands/read.ts` + `cli/commands/history.ts` |
| 检 | `bm25Query/semanticArm` 永远不扫 `inbox`，`hybrid.ts` 终榜 `annotateHits` 注入 `provenance` 供按需再取 | `retrieve/hybrid.ts` + `retrieve/query.ts` |
| 参考 | `codex-rs/memories/write/src/storage.rs:22 sync_rollout_summaries` 的 `rollout_path + rollout_slug` 索引同构 | — |

写事务遵循 `AGENTS D18`：`holdLock → write md → sync hook → dirty → history_index` 同批次 `flush`，不二次 `commit`。

## 4. 验收

| ID | Given / When / Then |
|---|---|
| P133-01 | `remember` 后 `note` 含 `provenance` |
| P133-02 | `read --with-history` 可回跳 `turn` 原文 |
| P133-03 | `query` 不命中 `inbox` 文本 |

## 5. DoD

- [x] P133-01–03 绿

## 6. 明确不做

- 见 §2
