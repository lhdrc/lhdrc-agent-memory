# Spec P13.4 — Prompt/模板参照 Codex 重构

| 字段 | 值 |
|---|---|
| ID | P13.4 |
| 状态 | draft |
| 依赖 | P6.6、P8.4 |
| 对应架构 | 08 §6.3；TODO **#54** |
| 仓库 | 本仓 `resources/*` |

> 抄 `codex-rs` 门控与字面保留，`read` 不照搬 `read_path.md`。

## 1. 目标

1. 首部插入 `NO-OP Gate: Will future agent act better?` 四条空转判。  
2. 4 桶：稳定偏好/高杠杆捷径/任务映射/环境证据；`Preference signals` 保留 `when user said "<quote>" -> future default`。  
3. `success/partial/fail/uncertain` 分流。  
4. `abstract/overview` 加 `preserve original wording before compress`。  
5. `read_path.md` 文案译成 `shouldQueryMemory` 门控与 `query --explain`。

## 2. 非目标

- 全量两阶段 `raw_memories→MEMORY.md`
- `read` 改三工具
- 按项目训模型

## 3. 技术方案

| 改动 | 文件 |
|---|---|
| Gate/4桶/分流 | `resources/session-extract-v1.md` |
| 字面保留 | `resources/abstract-v1.md` + `overview-v1.md` |
| 门控文案 | `retrieve/query-triggers.ts` + CLI help |

## 4. 验收

| ID | Given / When / Then |
|---|---|
| P134-01 | 无意义会话回 `{"items":[]}` |
| P134-02 | 偏好含原话 `when user said` |
| P134-03 | 摘要保数/名 |

## 5. DoD

- [ ] P134-01–03 绿

## 6. 明确不做

- 见 §2
