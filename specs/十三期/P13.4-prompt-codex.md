# Spec P13.4 — Prompt/模板参照 Codex 重构

| 字段 | 值 |
|---|---|
| ID | P13.4 |
| 状态 | done |
| 依赖 | P6.6、P8.4 |
| 对应架构 | 08 §6.3；TODO **#54** |
| 仓库 | 本仓 `resources/*` |

> 抄 `codex-rs` 门控与字面保留，`read` 不照搬 `read_path.md`。

## 1. 目标

1. 首部插入 `NO-OP Gate: Will future agent act better?` 四条空转判。  
2. 4 桶：稳定偏好/高杠杆捷径/任务映射/环境证据；`Preference signals` 保留 `when user said "<quote>" -> future default`。  
3. `success/partial/fail/uncertain` 分流。  
4. `abstract/overview` 加 `preserve original wording before compress`。  
5. `read_path.md` 文案译成 `shouldQueryMemory` 门控与 `query --explain`。

## 2. 非目标

- 全量两阶段 `raw_memories→MEMORY.md`
- `read` 改三工具
- 按项目训模型

## 3. 技术方案（对标 `codex-rs/memories/write/templates/memories/stage_one_system.md:28 NO-OP Gate / 48 High-signal 4桶 / 99 How to read rollout User>Tool>Assistant / 150 Outcome triage / 304 evidence->implication + 221 rollout_summary/raw_memory` / `consolidation.md:357 Wording-preservation` / `ext/memories/templates/memories/read_path.md:17 Decision boundary` / `write/src/prompts.rs:110` 70% 窗口截断）

| 改动 | 文件 | 细节 |
|---|---|---|
| Gate/4桶/分流 | `packages/core/resources/session-extract-v1.md` | 首部插 `Before return ask: Will future agent act better? If one-off query/no durable insight → {"items":[]}` + `high-signal 4桶：稳定偏好/高杠杆捷径/任务映射/环境证据` + `User>Tool>Assistant` 阅读序 |
| 偏好句式 | 同上 | 要求 `when user said "<quote>" -> future default` 保留原话，`Preference signals` 单拎，不与 `Reusable knowledge` 混 |
| Outcome | 同上 | `success/partial/fail/uncertain` 据 `Failures` 多写何不 work、`partial` 少写复现，抄 `stage_one_system.md:150` |
| 字面保留 | `packages/core/resources/abstract-v1.md` + `packages/core/resources/overview-v1.md` + `packages/core/resources/extract-v1.md` | 加 `preserve original wording before compress; keep names/numbers; no invent`，`overview` 加 `Prefer original wording over vague summary` |
| 门控文案 | `packages/core/src/retrieve/query-triggers.ts` + `packages/cli/src/commands/query.ts` help | 译 `read_path.md:12 Skip ONLY self-contained / Use by default when mentions workspace` |
| 窗口 | `packages/core/src/compile/session.ts:102` | 按模型 `context_window*70%` 截断 `turns`，对标 `prompts.rs:110`，替代固定 `16000 chars` |

## 4. 验收

| ID | Given / When / Then |
|---|---|
| P134-01 | 无意义会话回 `{"items":[]}` |
| P134-02 | 偏好含原话 `when user said` |
| P134-03 | 摘要保数/名 |

## 5. DoD

- [x] P134-01–03 绿

## 6. 明确不做

- 见 §2
