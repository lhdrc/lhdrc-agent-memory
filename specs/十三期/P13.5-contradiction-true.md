# Spec P13.5 — 真矛盾标记（非 duplicate 相似）

| 字段 | 值 |
|---|---|
| ID | P13.5 |
| 状态 | draft |
| 依赖 | P10.3、P11.6、P9.2 |
| 对应架构 | 08 §8.3；TODO **#55**；对标 `gbrain/src/core/facts/classify.ts:3` |
| 仓库 | 本仓 `packages/core` |

> 对标 `gbrain` Hot Memory `classifyAgainstCandidates:68`：实体桶 `k=5` + `0.95/0.92` + `LLM三分类` + 人审 `*0`。

## 1. 目标

1. 桶：`倒排 fts + links + entity_registry` 按 `entity_slug` 分桶，`k=5`。  
2. 快路径：桶内 `cosine≥0.95→duplicate`。  
3. 值冲突：`isObjectValueConflict + 否定词` 二筛。  
4. LLM：灰区 `0.92-0.95` 批量10对 `duplicate|supersede|independent`。  
5. 落盘：`contradictions.md` 标 `contradiction supersede a->b` + 预填 `facts.supersedes`，人审 `keep a|b` 才 `superseded` 入索引剔除 `*0`。  
6. 增量：`dream --phases 4` 仅新 `facts` + 同桶邻居。

## 2. 非目标

- 全量 `O(n²)` LLM 每对判
- `local` 强行 LLM
- `k>5`、自动 `purge` 整篇

## 3. 技术方案

| 规划 | 落点 | 对标 |
|---|---|---|
| 桶 | `retrieve/graph.ts` + `index/sync.ts` | `classify.ts:3①` |
| 快路径 | `dream/runner.ts:222` | `classify.ts:92` |
| 值冲突 | `write/dedupe.ts:40` | 本仓复用 |
| LLM | `complete()` 批量 | `classify.ts:105` |
| 落盘 | `contradiction/review.ts:73` + `sync.ts:54` | `engine.insertFact` |

## 4. 验收

| ID | Given / When / Then |
|---|---|
| P135-01 | 同实体异值 `NY vs SF` 标 `contradiction` 非 `duplicate` |
| P135-02 | `local` 档走规则分支不进 LLM |
| P135-03 | 灰区批量 LLM 后 `contradictions.md` 含 `supersede` |

## 5. DoD

- [ ] P135-01–03 绿

## 6. 明确不做

- 见 §2
