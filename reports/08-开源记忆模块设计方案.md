# df-memory 开源记忆模块 详细设计方案

> **文档定位**：架构与 ADR。**文件为权威存储 + 检索索引 + 蒸馏 + 结晶 + 混合注入**，面向"开源、单机、通用、本地部署"。
> **权威顺序**：编码与验收以 [`specs/`](../specs/) 为准；本文与 Spec 冲突时 **先改 Spec/本文 ADR，再改代码**（见 `AGENTS.md`）。差距账本见 [`TODO.md`](../TODO.md)。
> **前置阅读**：`06`（架构总览）、`01`~`05`（四项目调研）、`07`（关系型方案，本方案为演进版）
> **日期**：2026-08-02（P0：2026-08-06；D1/D18：2026-08-06；**落地修订：2026-08-18**，对齐九期实现与明确裁剪）｜ 语言：中文
> **状态**：ADR 已按实现修订。MVP→九期主线 **done**；十期 P10.1 LoCoMo 发数 **in_progress**。下文凡写「原设计 / 未交付」的段落，不得当现行接口。

---

## 0. 已锁定决策（ADR）

| # | 决策 | 理由 |
|---|---|---|
| D1 | **记忆 = md 文件 + frontmatter 为权威**；PGLite/pgvector 为检索索引（可丢、可 `rebuild-index`）；**git 不是写路径必要条件**，仅为可选版本账本（见 D18） | 对齐 GBrain：磁盘工作区 = 真相；索引 = 对象库投影；人类可读/本地可跑；版本史可选 |
| D2 | **brain/source 双轴隔离**：brain=访问边界、source=内容边界；**凡 brain 级产物（entities/events/experiences/skills）一律落在 `brains/{brainId}/` 下**；根目录禁止放租户内容；开源默认一 brain 一仓（**推荐仍 `git init`，便于账本/clone**） | 隔离由路径强制；多任务经验汇聚到项目脑且不串租户 |
| D3 | **蒸馏决策走 OpenViking**：LLM 两级去重判定（candidate/item）+ memory_diff 审计；**item:delete = 经验层 superseded，禁止删源文件**（见 D17） | 蒸馏"该用哪些信息"最实证、最可审计，且不破坏文件权威 |
| D4 | **框架查询走 GBrain 思路**：混合检索 + RRF（P9.3 rescale）+ 零 LLM 图谱 + dream **五段** | 检索与维护工程化；九段 dream **裁剪** |
| D5 | **Skill 结晶走 MemOS Reflect2Evolve**：经验成熟 → 结晶 SKILL.md → 注册技能体系 | 从"注入经验"升级为"沉淀能力" |
| D6 | **注入 = 混合式**：启动注入 top-3 **经验**（P9.6）+ 运行时主动 query；skill **按需**（P8.3），不混默认检索 | 被动保证飞轮转；不做 Skill>经验>源 系统级联 |
| D7 | **agent 访问面 = CLI + DSH 插件工具，不把直读仓文件当主 API**；**MCP / REST（P4.1）明确不做** | 文件可读性给人和运维；工具面已落地为 `memory` CLI 与并列仓 `dsh-df-memory`（P4.2），不经 MCP |
| D8 | **开源 MIT + 单机部署 + 本地可跑** | 用户决策；类比 GBrain 单机成功路径 |
| D9 | **通用核心 + 适配器**：df-app 通过摄取适配器插件接入，不硬编码进核心 | 开源通用性；本仓已有 `ingest-generic-jsonl` / `ingest-df-app` / `ingest-session` |
| D10 | **交付技术栈锁定 TypeScript + Bun**；**Java 17 方案明确不做** | 原「双方案并行出稿」已关；接口契约仍可参考，不再维护 Java 实现 |
| D11 | 零 LLM 能跑：链接/意图/关系解析用确定性正则，LLM 只用于富化 | GBrain 成本哲学 |
| D12 | 成本第一等公民：search mode 档位 + cost cap + kill-switch + 降级链 | GBrain |
| D13 | **四存储原语跨 pack 通用**：entity / event / fact / relation 均有文件落位，索引仅投影；**Entity merge = 文件事务（canonical + redirect + event），禁止只改索引** | 身份去重可重建；「不是正文字节 merge」≠「可以不写文件」 |
| D14 | **写入格式即规格**：`WRITE_FORMAT.md` 约束所有写入入口，系统强制校验而非靠 AI 自觉 | 写库规定格式，AI 决策写入有据可依 |
| D15 | **单机多租户**：一实例多 brain；`AccessControl` fail-closed + 路径前缀；插件 per-call `brain` | REST API token **未做**（无 MCP/REST）；git 历史对同仓可见，非密码学隔离 |
| D16 | **agent 适配器仅 DeepSeek Harness（DSH）**；Claude Code / Codex / OpenCode / OpenClaw / Cursor 模板 **不做** | 混合注入落到 DSH 插件（P4.2 + P8.1）；P6.5 Cursor hook **不做** |
| D17 | **四层写语义**：L0 热路径 ADD-only；L1 蒸馏只写 experiences/；L2 dream 只软归档/标矛盾；L3 forget 默认软删，硬删需 owner+purge | 消除 Mem0 / OpenViking / dream 三套哲学冲突；热路径不删源 |
| D18 | **git 批量账本（热路径不借 git）**：写入 = 锁 → 写 md → `content_hash` 增量同步索引 → 标记 dirty；**默认不每写 `git commit`**。Flush 触发：条数 N（默认 20）∨ 时间 T（默认 5min）∨ 显式 `memory sync --commit` ∨ 进程正常退出 best-effort ∨ **关键操作强制即时 commit**（entity merge / schema use / purge）。`mode: off\|batch\|per_write`，MVP 默认 `batch` | 降 Windows/CI 成本与 CRLF 噪音；幂等靠 hash；保留审计/clone/revert；对齐 GBrain「类 git」心智而非「逐写调用 git」 |
| D19 | **embedding 三档（P9.2）**：`init` 默认 `openai`；hermetic CI 用 `local` 哈希；`onnx` 为真本地模型。无 Key / 出网失败 **fail-open 哈希**，不阻塞 BM25+图。`off` 关闭语义臂 | 原 §5.6「默认本地小模型」已废止；不把哈希冒充 onnx 成功 |

### 0.1 落地对照（2026-08-18）

> 原设计里的「全量愿景」仍可读；下列项已按实现 **修订 ADR 或明确裁剪**，不得再当现行规格。

| 项 | 现行 |
|---|---|
| 访问面 | **CLI** + **DSH 插件**（query / read / remember / job / skill）。MCP / REST / `memory serve` **不做** |
| 技术栈 | 仅 TS/Bun；Java **不做** |
| 写路径 | `capture` / `remember` / `ingest session` **默认入队**（P9.8）；立刻可查用 `--wait` |
| LLM | 会话/蒸馏走 `complete()`；`llm.provider` init 默认 `off`；无 Key → `E_DISABLED`（escape：`remember --no-extract`） |
| embedding | 见 D19；不是默认 Ollama/bge |
| 索引表 | 仅 `pages` / `chunks` / `entity_registry` / `links` / `search_cache`。**不建** `facts` / `event_ledger` 表（facts 在节点 frontmatter；事件在 `events/*.jsonl`） |
| schema pack | **仅** `problem-tree`；topic-tree / wiki-tree **未交付** |
| facts.md | **不做**；facts 只在节点 frontmatter |
| compiled_truth / synopsis | **不做** |
| dream | **v1 五段**（lint / sync / distill_pending / contradictions / orphans）；九段 **明确裁剪** |
| 矛盾分类 | dream 启发式标 `contradictions.md`；cosine+LLM 三分类 **未做** |
| 注入 | 启动注 **top-3 active 经验**（P9.6）；skill **按需**（P8.3），不混默认 query；不做 Skill>经验>源 系统级联 |
| query 扩写 | tokenmax **LLM 扩写不做**（调用方已是模型）；真模型 rerank **默认关** |
| 评测 | hermetic `eval:mini` / `distill` / `report` + LoCoMo **fixture**（P5.6）；对外发数走 P10.1 `locomo10.json`。LongMemEval / HaluMem **不做** |
| 主线进度 | MVP–九期 **done**；P4.2 A 接线未 npm publish；P10.1 G0 runner 已交、全量发数待 G1–G6 |

---

## 1. 项目定位

**一句话**：开源、单机、通用、本地部署的记忆模块——"agent 的 git + 知识库"。任何团队/个人 `clone` 即可运行，把多 source 的任务经验蒸馏、结晶、注入成一个可用的项目脑。

> **「git」在口号中的含义（D1/D18）**：产品体验像版本化知识仓（可 clone、可审计）；**实现上热路径以文件 + hash 索引为权威**，git 为可选批量账本，而非每次写入都调用 git。

**目标用户**：
- 直接用户：用 CLI 或 **DSH agent** 的个人/团队；
- 集成方：通过 **ingest 适配器** 把消息流写入本仓（df-app fixture 已有）。Claude Code / Codex 等 harness **未做适配器**。

**非目标**（YAGNI）：
- 不做分布式/多实例（**单机部署**，但**多租户是目标**——单实例内用 brain/source + 权限支持多团队，OpenViking 同款思路）；
- 不做对外商业托管（开源自托管，托管留给生态/未来）；
- 不做多模态记忆（纯文本优先，图像/工具轨迹后置）。

---

## 2. 设计原则（四项目精华）

| 原则 | 来源 | 含义 |
|---|---|---|
| 记忆 = 文件树 + 知识图谱 | GBrain + OpenViking | md 文件确定性寻址 + 零 LLM typed edges 关系推理 |
| 写入只增不覆盖（ADD-only，热路径） | Mem0 | **仅 L0 热路径**：提取单遍、只增 facts/节点；更新/归档由 D17 分层承载 |
| 写入时分层，读取时按需 | OpenViking | L0 摘要/L1 概览/L2 全文，检索按层注入省 token |
| 蒸馏"该用哪些信息" | OpenViking | LLM 两级去重判定 + memory_diff 审计，可回滚；**不删源文件**（D17） |
| 零 LLM 也能跑 | GBrain | 链接/意图/关系解析确定性正则；LLM 只用于富化 |
| 异步写入 + 可观测 | MemOS + OpenViking | 写入不阻塞，事件总线可观测 |
| 经验结晶为能力 | MemOS | 经验成熟 → SKILL.md → 技能体系 |
| 成本第一等公民 | GBrain | search mode + cost cap + 熔断器 |
| 隔离是架构保证 | GBrain | brain/source 双轴 + **路径前缀强制**（brain 级内容不得落根目录） |

---

## 3. 总体架构

### 3.1 架构图

```
┌──────────────────────────────────────────────────────────────┐
│   人 / 脚本                    DSH agent（并列仓插件）          │
│        │                              │                      │
│     CLI `memory`              插件工具（非 MCP）               │
└────────┼──────────────────────────────┼──────────────────────┘
         ▼                              ▼
┌──────────────────  @lhdrc/core（单机）  ───────────────────────┐
│  JobRunner（.dfmemory/jobs/）· 写队列 · AccessControl          │
│  管线：摄取 → compile/extract → 蒸馏 → 分层 → 建图 → 结晶      │
│  ┌──────────────────────┬───────────────────────────────────┐ │
│  │ 权威存储：md 文件树      │ 检索索引：PGLite（默认）/ Postgres │ │
│  │ git：可选批量账本（D18） │ pgvector HNSW **未做**（BYTEA 余弦）│ │
│  └──────────────────────┴───────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
                  ▲ 摄取适配器（本仓 packages/adapters）
          generic-jsonl / df-app fixture / session jsonl
```

> **不在图上**：MCP server、REST、`memory serve`、Claude Code / Cursor harness。

### 3.2 核心数据流

```
写：CLI / DSH 工具 → JobRunner 入队（默认；`--wait` 同步）
  → 单写者串行化队列 → 写 md → content_hash 增量同步索引 → 标记 git dirty
  →（满足 D18 flush 条件时）批量 git add/commit
  → 可选懒蒸 / 分层 / 建图（compile 后，受 kill-switch）
读：检索走索引（快）→ 分层标注 snippet（P8.2）；浏览走文件树
维护：`memory dream` 五段（非夜间九段 cron）
```

---

## 4. 核心概念：brain/source 双轴（GBrain）

### 4.1 双轴心智模型

- **Brain = 访问边界**：一个记忆库实例默认一个 brain（开源默认：**一 brain 一仓**，推荐 `git init` 作账本）；同进程可挂载多个 brain（各仓独立或单仓多 `brains/{id}/` 前缀）。对应"一个项目脑"。
- **Source = 内容边界**：brain 内多个 source（session/issue/任务），slug 按 source 唯一。
- **落位铁律（D2）**：凡标注「brain 级」的内容，路径必须在 `brains/{brainId}/` 下；根目录只放实例内部状态，禁止放租户记忆。

```
memory-root/                              ← 记忆根（文件权威；推荐 git init 作账本；默认 = 单 brain）
├── .dfmemory/                            ← 实例内部状态（游标、索引 meta、pglite；非记忆内容；勿提交索引）
└── brains/{brainId}/
    ├── brain.yml                         ← brain 配置（id、name、描述、权限、可选 mounts）
    ├── sources/{sourceId}/
    │   ├── .dfmemory-source              ← source 路由配置
    │   ├── issues/{issueId}/             ← 默认 schema pack 形状（可换）
    │   │   ├── .meta.json
    │   │   ├── requirement.md
    │   │   ├── decisions/{n}-*.md
    │   │   └── lessons.md
    │   └── ...
    ├── entities/{slug}.md                ← 四原语：实体身份（含 redirect stub）
    ├── events/YYYY-MM/{slug}.jsonl       ← 四原语：事件账本
    ├── experiences/{id}.md               ← 跨 source 蒸馏层（brain 级）
    ├── skills/{name}/SKILL.md            ← 结晶技能（brain 级）
    └── contradictions.md                 ← dream 矛盾待裁决（brain 级）

# 可选（实例级，显式授权才可见）：
shared/skills/{name}/SKILL.md             ← 仅当 brain.yml mounts 声明时对该 brain 可见
```

> **注意目录形状**：上面的 `issues/{issueId}/`、`decisions/`、`lessons.md` 是**默认 schema pack（问题树模板）**，不是核心硬编码——见 §4.4。核心只认"source 下任意目录树 + frontmatter schema"。**df-app 适配器负责把 workspace/issue 映射到这里的 brain/source/路径**，df-app 形状不进核心。
>
> **多 brain 与 git**：开源默认一 brain 一仓，权限最简单。若单仓多 brain，隔离仍靠 `brains/{id}/` 前缀 + API scope；**git 历史对同仓成员可见**——不得伪装为强隔离。

### 4.2 source 解析链（7 层，GBrain 移植）

```
--source flag → env → .dfmemory-source dotfile → 路径前缀匹配
→ brain 级 sources.default → sole_non_default（单 source 自动路由）→ 字面 'default'
```

所有读侧 op 走 `sourceScope(ctx)` 强制 source 过滤（GBrain `sourceScopeOpts`），漏一线即跨 source 泄漏。brain 级路径（entities/experiences/skills/…）另走 `brainScope(ctx)`，禁止跨 `brains/{id}/` 前缀。

### 4.3 你的范式的映射

```
多个 source（任务/session/issue，粒度由适配器/schema pack 决定）各自沉淀
  → 蒸馏层（brains/{id}/experiences/）汇聚跨 source 可复用经验
  → 同一 brain 读 experiences/ + 检索本 brain 下各 source 的蒸馏物
  → 成熟经验 → 结晶 brains/{id}/skills/*/SKILL.md → 注入新任务
```

### 4.4 schema pack：核心通用，形状可配（重要）

**核心不硬编码任何路径语义**。目录结构、schema_type、merge_op、文件名模板全部由 **schema pack** 定义：

- **默认包 `problem-tree`**（问题树）：`issues/{id}/requirement|decisions|lessons` —— **现行唯一交付 pack**；
- **备选包**（`topic-tree` / `wiki-tree` 等）仍是贡献方向，**未实现、不进 DoD**；
- 新包 = 一个 YAML 声明（目录模板 + schema_type 清单 + 文件名模板 + `merge_op`），`memory schema use <pack>` 切换（目前仓内只有 problem-tree）。

核心（引擎/检索/蒸馏/结晶）**只消费 frontmatter 里的 `schema_type` 与通用字段**，不感知具体路径语义。df-app 适配器只做"workspace→brain/source、issue→路径"的映射，不要求核心认识 issue。

### 4.5 四存储原语（跨 pack 通用的结构层）

> 目录树 + frontmatter 是**可读面**；下面四个原语是**结构面**（GBrain `GBRAIN_RECOMMENDED_SCHEMA.md:82-88` 模式），跨所有 schema pack 通用，是核心的一部分——用户改 schema pack 改不了它们。**落位一律在 `brains/{brainId}/` 下**（D2）。

| 原语 | 存储 | 作用 | 文件落位 |
|---|---|---|---|
| **Entity registry** | `entities/` + 索引 `entity_registry` | 实体身份：canonical ID + 全部别名 + 外部 ID；**合并 = 文件事务**（见下），索引仅投影 | `brains/{id}/entities/{slug}.md`（活实体或 redirect stub） |
| **Event ledger** | `events/*.jsonl`（**无** `event_ledger` 索引表） | 不可变事件流（meeting/email/capture/update/entity_merged） | `brains/{id}/events/YYYY-MM/{slug}.jsonl`（append-only） |
| **Fact store** | 节点 frontmatter `facts` 块（无独立 `facts.md`、无 `facts` 表） | 带 provenance 的结构化声明；矛盾 = 两个不同值的 fact | 所属 source 节点 md |
| **Relationship graph** | 节点 frontmatter `links` + 索引 `links` | 类型化边，支撑图查询 | §5.1 links |

**为什么四原语在文件层有落位**：文件权威原则要求**每个原语都能从文件重建**——`rebuild-index` 时四原语全部从 `entities/`（含 redirect）、`events/`、frontmatter `facts`/`links` 重扫，索引只是投影。

#### 4.5.1 Entity merge = 文件事务（D13，废止「只改 DB」）

> 原文「合并 = 数据库操作，不是文件合并」已废止。正确含义：**不是把两篇正文拼成一篇**，而是**身份关系必须落文件**，否则 `rebuild-index` 会复活分裂实体。

合并入单写者队列，原子步骤：

| 步骤 | 文件动作 | 说明 |
|---|---|---|
| 1. 选 canonical | 默认**人工确认**；规则选主仅作建议 | 身份误判是第一失败模式 |
| 2. 写活实体 | 合并 `aliases` / `external_ids`（及可选事实引用）到 `entities/{canonical}.md` | 权威身份只存在于文件 |
| 3. 写 redirect | loser → `status: merged` + `redirect: {canonical}`，**不物理删除** | 旧路径可解析；可回滚 |
| 4. 记事件 | `events/…` append `entity_merged`（from/to/by/at） | 审计依据 |
| 5. 投影索引 | `entity_registry`：alias→canonical；软删 loser 行 | 索引可丢，文件不可丢 |
| 6. git flush | **强制即时 commit**（D18 关键白名单） | 保证 `git revert` 能对准整次 merge |

**resolve**：索引快路径；miss 时扫 `entities/` + 跟随 redirect 链（depth≤2）。

**验收**：`merge → 删除索引 → rebuild-index` 仍解析到同一 canonical；在 `git.mode≠off` 时，`git revert` 该次 merge 的强制提交后身份分裂可恢复。

**身份去重心智**：两个 alias 指向同一 canonical ID——通过 redirect/aliases 文件表达，**禁止只 UPDATE 索引**（GBrain 承认分裂页是第一失败模式，`GBRAIN_RECOMMENDED_SCHEMA.md:158`）。

---

## 5. 存储设计（文件权威 + 检索索引）

### 5.1 权威存储：md 文件 + frontmatter

每个记忆节点 = 一个 md 文件，frontmatter 承载结构化 meta：

```markdown
---
title: 重试策略调整
schema_type: decision          # requirement/decision/lesson/experience/session/...
source: issues/1024            # 归属 source
path: issues/1024/decisions/1-重试策略调整
version: 1
status: active                 # active/archived/stale
created_by: agent:claude
created_at: 2026-08-02T10:00:00Z
tags: [payment, timeout]
aliases: [重试策略]
links:                          # 零 LLM 建边（frontmatter 声明 + 正文 wikilink）
  - { to: projects/payment, type: belongs_to, source: path }
  - { to: members/alice, type: mentions, source: mention }
facts:                          # 原子事实（文件即真相的一部分，同步进索引）
  - text: 重试策略由指数退避改为固定 3 次
    event_type: decision
    attributed_to: agent:claude
    at: 2026-08-02
  - text: 超时阈值由 2s 调整为 5s
    event_type: decision
    attributed_to: agent:claude
    at: 2026-08-02
---
## 摘要      (L1 overview 的源)
## 决策内容   (L2 全文)
指数退避改为固定 3 次退避，超时阈值 2s→5s。
[[decisions/2-超时阈值]] 相关。[[projects/payment]]
```

**frontmatter 即 schema**：`schema_type` 决定解析器/merge_op/文件名模板——**全部来自 schema pack（§4.4），核心只认通用字段**。示例里的 `issues/1024/decisions/1-重试策略调整` 是默认 `problem-tree` 包的形状，换成 `topic-tree` 包就是 `topics/foo/notes/...`，核心代码不用改。无迁移负担——加字段即演进。

**facts 落位**：**只写所属节点的 frontmatter `facts` 块**（上例）。每 source 一份 `facts.md` **不做**。文件持有 facts 权威副本；**索引不建 `facts` 表**（P9.5 趋势查询扫 md）。`rebuild-index` 重扫 frontmatter，不丢事实。facts 可选量纲字段 `metric` / `value` / `unit` / `period`（`memory trend`）。

### 5.2 检索索引（PGLite/pgvector）

| 索引表 | 内容 | 用途 |
|---|---|---|
| `pages` | 文件 meta + title/body/ngram + frontmatter JSON + `content_hash` | 页面级检索（含 experiences/skills 文件行） |
| `chunks` | 正文分块 + embedding（BYTEA；postgres 真 `vector`/HNSW **未做**） | 向量检索 |
| `links` | typed edges（from/to/type/source） | 图谱遍历 |
| `entity_registry` | 实体 canonical + 别名（同步自 `entities/`） | 身份去重 |
| `search_cache` | 查询缓存（`knobs_hash`） | 防污染缓存 |

> **不建表（明确裁剪）**：`facts`、`event_ledger`、独立 `experiences`/`skills`/`change_log` 表。facts 在 frontmatter；事件在 `events/*.jsonl`；经验/技能是 md，投影进 `pages`；`memory_diff` 走文件审计（L0 capture 的 diff 覆盖仍是 TODO 下期项）。

> **索引全部是派生数据**（可重建）。失败不回退、不丢 md（D1）。引擎：**PGLiteEngine**（默认）与 **PostgresEngine**（`index.engine: postgres` + `DF_MEMORY_DATABASE_URL`，P5.7）。见 [`specs/五期/P5.7-postgres-engine.md`](../specs/五期/P5.7-postgres-engine.md)、[`scripts/dev-postgres.md`](../scripts/dev-postgres.md)。

### 5.6 embedding 提供方（P9.2，改 ADR）

> **废止**「默认本地 bge / Ollama」。现行三档 + `off`：

| 档 | `embedding.provider` | 场景 |
|---|---|---|
| **init 默认** | `openai`（OpenAI 兼容 HTTP；可配 `base_url` / `model` / `dims`） | 语义质量；无 Key 时读路径 **fail-open 哈希** |
| 真本地模型 | `onnx` | 离线权重；缺文件 **不得**用哈希冒充成功 |
| CI / hermetic | `local` | 确定性 bigram 哈希，不出网 |
| 关语义臂 | `off` | 只走 BM25 + 图 |

embedding 与 LLM **分提供方**（`createEmbeddingProvider` ≠ `LLMProvider.embed`）。`memory.yml` 切换；成本/延迟分别统计。写路径缺 Key 时不得把哈希向量写入「meta 声称 openai」的索引（mismatch → 提示 `rebuild-index --embeddings`）。

### 5.3 同步机制（文件 → 索引）

对齐 GBrain「类 git」心智（磁盘 = 工作区，索引 = 对象库，`content_hash` = blob）：

- 每次文件写入/软删后**增量同步**（进程内）：读文件 → 算 `content_hash` → 与索引行比较；**哈希相同则跳过**（幂等短路，不重分块/不重嵌）；
- hash 只吃**语义白名单字段**（P9.1）：剔除纯时间戳型 frontmatter（如 `captured_at`），语义不变不重切块；
- 物理删除文件（若发生）→ 索引软删除/墓碑（`status`/`deleted_at` 语义）；MVP `forget` 默认只改 `status=archived`，文件仍在；
- 索引可**全量重建**（`rebuild-index`，从文件权威恢复）——索引是派生数据，丢了能重建，**文件是真相**。

### 5.4 git：可选批量账本（D18）

> **热路径不借 git 做文件管理。** 写事务的原子边界是「单写者锁 + 磁盘 md + 索引投影」，不是 `git commit`。
> git 提供审计 / clone 备份 / revert；未 flush 的 dirty 工作区**不是**丢权威，只是尚未进入版本史。

#### 5.4.1 类 git 三区对照（GBrain）

| git | df-memory | 说明 |
|-----|-----------|------|
| 工作区 | 磁盘 md 树（`brains/**`） | **权威**；用户/CLI 直接读写的原文 |
| 对象库 | PGLite `pages`/`chunks`/`entity_registry` 等 | 检索投影；可丢可重建 |
| blob 哈希 | `content_hash`（SHA-256） | 内容寻址、变更判定、幂等短路 |
| index 路径映射 | `path` / `source_id` 列 | 相对仓根路径 → 页面 |
| 提交历史 | `git log`（flush 后） | 可选账本，非热路径 |

#### 5.4.2 Flush 触发（默认 `mode: batch`）

| 触发 | 默认 | 行为 |
|---|---|---|
| 条数阈值 N | **20** 次成功写入累计 dirty | 一次 `git add` 约定路径 + 一个 commit |
| 时间阈值 T | **5 min** 且仍有 dirty | 同上 |
| 显式命令 | `memory sync --commit`（或 `memory commit`） | 有 dirty 才提交；无则 no-op |
| 进程退出 | CLI 正常退出 | best-effort flush（失败只 warn，不回滚已写文件） |
| 关键强制 | entity **merge**、`schema use`、**purge** | **立刻单独 commit**（便于 `git revert`） |
| 不触发 | hash 未变的 no-op、纯 query/read/`rebuild-index` | 禁止空 commit |

满足 **N ∨ T ∨ 显式 ∨ 退出 ∨ 关键** 任一即可；关键操作**插队**，不与普通 capture 混在同一 squash（除非同一次强制 flush 窗口内仅含该关键事务——实现上关键路径单独 commit）。

一次 flush = **一个 commit**，包含窗口内全部 dirty 路径；消息形如 `memory: flush 12 files` 或摘要 `+3 decision`；merge 单独：`memory: entity merge bob -> alice`。

**勿提交**：`.dfmemory/pglite/`、写锁、可丢的 index-meta（已由 `.gitignore` 约束）。

#### 5.4.3 配置（`memory.yml`）

```yaml
git:
  mode: batch              # off | batch | per_write（per_write 仅兼容/调试）
  auto_commit: true        # batch 下启用 N/T 自动 flush；off 时忽略
  commit_prefix: "memory:"
  batch_size: 20
  batch_interval_ms: 300000
  force_commit_on:
    - entity_merge
    - schema_use
    - purge
```

| `mode` | 含义 |
|---|---|
| `batch` | **MVP 默认**：热路径不 commit；按上表 flush |
| `off` | 永不自动 commit；仅显式命令（无 git / 极简环境） |
| `per_write` | 旧行为：每次成功写入都 commit（迁移/对照用） |

#### 5.4.4 风险边界（对用户说清）

- **丢索引** → `rebuild-index`，文件仍在。
- **丢未 commit 的工作区** → 丢数据（与任何未备份目录相同）；需要版本史就依赖 flush / 显式 sync / 关键强制 commit。
- **并发**：仍由单写者队列 + 文件锁串行；**不**用 git merge 解决多写者冲突。

---

## 6. 写入管线

### 6.0 四层写语义（D17，重要）

> 同时引入 Mem0 ADD-only、OpenViking merge/delete、dream archive 时，必须先排优先级——**谁能对源文件做什么**。

| 层 | 触发 | 允许写什么 | 禁止 |
|---|---|---|---|
| **L0 热路径** | `remember` / `capture` / ingest session（**默认入队**） | ADD-only：新建节点、append facts/links | merge 源节点、delete、改他人 source |
| **L1 蒸馏** | 异步 `refine` / 懒蒸 | **只写** `brains/{id}/experiences/`：skip \| create \| merge 经验投影；读 pack `merge_op`（P9.9） | 删/改 `sources/` 下源 md；「delete」= 归档旧经验版本 |
| **L2 维护** | `memory dream`（五段） | 标 contradiction、orphan→`archived`、提 skill candidate | 无人工确认的硬删除；无确认的 entity merge；**不**跑九段 backlink/embed |
| **L3 显式遗忘** | `memory forget` / 人工 | `status=archived`（软删）+ event | 默认物理 `rm`；硬删需 `--purge` + owner |

**OpenViking 词表重映射**（D3 补丁）：

| 原判定 | 落层 | 文件效果 |
|---|---|---|
| candidate: skip | L1 | 不写经验；源事实保留 |
| candidate: create | L1 | 新建 `experiences/{id}.md` |
| item: merge | L1 | 合并进已有经验 md（经验层 merge_op） |
| item: delete | L1 | 旧经验 `status=superseded`；**源文件不动** |
| cosine duplicate | L0 快路径 | 跳过写入；零 LLM |
| dream orphan | L2 | 临时页 `archived`；硬删不在 dream 默认路径 |

**冲突裁决**：同 path 并发 → 单写者串行；事实矛盾 → 两 facts 并存并写入 `contradictions.md`，不自动删；注入：**启动只注 top 经验**，skill 按需（P8.3），源记忆不自动注入。Iron Law back-link + facts `[Source:]` 走 **写后 enrich**（P9.7），不扩 dream。**memory_diff 覆盖 L1–L3**；L0 capture 的 `op: create` 仍为下期项。

### 6.1 三种写入入口

| 入口 | 方式 | 场景 |
|---|---|---|
| **CLI** | `memory capture` / `import` / `remember` / `ingest` | 人/脚本写入（**L0**）；默认入队，`--wait` 同步 |
| **DSH 插件** | `memory_remember` 等（并列仓，非 MCP） | agent 主动写入（**L0**）；与 CLI 共用 core JobRunner |

**MCP `memory_remember` / REST `POST /api/memory` 未实现（P4.1 不做）。**

所有写入**统一进单写者串行化队列**（保证**文件 + 索引**顺序一致；git flush 按 D18 另算）。CLI/插件热路径先入 `.dfmemory/jobs/`（P9.8）。L0 入口经 `validateWrite()` 后**只允许 ADD-only / append**（D17）。

热路径步骤（每个 job）：

```
持锁 → mutation 写 md → content_hash 同步索引
  → 标记 dirty（记入待 flush 集合）
  → 若命中 force_commit_on 或 mode=per_write → 立即 git commit
  → 否则仅检查 N/T 是否触发 batch flush
  → 放锁
```

索引 hook 失败：文件已落盘则 **不回滚文件**，warn + 提示 `rebuild-index`（D1）。

### 6.2 写入格式即规格（重要）

> **"写库规定写入的格式"**：所有入口共享同一套**写入格式规范**（`WRITE_FORMAT.md`，随代码版本管理），约束"什么能写、怎么写、写哪"——AI 决策写入时按此格式，平台适配器按此格式，人按此格式，三处一致。

**写入格式规范涵盖**：

| 项 | 规定 |
|---|---|
| **frontmatter 必填** | `title`/`schema_type`/`path`/`source`/`created_by`/`created_at`/`status` |
| **schema_type 来源** | 必须来自当前 schema pack 声明的类型清单（pack 未声明 → 拒绝） |
| **facts 块格式** | `text`/`event_type`/`attributed_to`/`at`（见 §5.1 示例） |
| **links 格式** | `to`/`type`/`source`（`source` ∈ wikilink/markdown/mention/verb） |
| **正文约定** | 结构化 markdown（`## 摘要` + 主体），供 L0/L1 分层解析 |
| **merge_op** | 由 schema pack 声明（immutable/patch/append），决定更新语义 |
| **拒绝规则** | 路径越界、非 UTF-8、超长、pack 未声明 `schema_type` 一律拒绝。敏感字段 mask **未做**（下期再锁范围） |

**AI 决策写入的落点**：DSH `memory_remember` / CLI `remember` 内部调用 `validateWrite()`（按 WRITE_FORMAT 校验）→ 通过才入队。校验失败返回结构化错误——**格式约束由系统强制，不靠模型自觉**。

### 6.3 提取（Mem0 ADD-only + GBrain 快路径，属 L0）

```
写入请求 → [快路径] 查相似现有事实（cosine≥0.95 → 跳过，零 LLM）
  → LLM 单遍提取（ADD-only，一次调用只做新增）
  → MD5 去重 → **写 facts 到所属节点 frontmatter**（无 `facts.md`）
  → 写 md 文件 + content_hash 增量同步索引（热路径；不强制 git commit）
  → 按 D18 标记 dirty / 条件 flush
  → 异步分层（L0/L1）+ 零 LLM 建图 + 写 change_log
  → （可选）入队 L1 蒸馏，不在本步改/删源文件
```

**prompt 即规格**：提取 prompt 作为资源版本管理（Mem0 理念），约束输出格式（facts/entities/attributed_to/event_type）。

> **六期补丁（会话原文）**：Agent 交的是 transcript，不是「写入请求」。入口是 `compileSession`（单写队列一次 job），不是 `capture`。先归档 `.dfmemory/inbox/`，再 `complete()` 填 schema，最后 `captureWrite` 写 L0。LLM 失败标 inbox `failed`，不写 L0。会话路径默认 `compile.dedupe_cosine=0.95`。详见 [`specs/六期/`](../specs/六期/)。人手 `capture` 仍走本节写后 enrich（P5.1）。  
> **P6.6**：提取 prompt 按 `decision`/`lesson`/`note` 写成类型合同；`complete` 前可只读 prefetch 已有标题（ADD-only，不 patch）。不引入 OV 的 11 类记忆或 ReAct 工具循环。

### 6.4 蒸馏决策（OpenViking，D3 + D17，属 L1）

写入/摄取后的异步蒸馏管线——**只读写 `brains/{id}/experiences/`，禁止触碰 `sources/` 源文件**：

```
Candidate Memories（提取产物，只读引用源）
  → 向量预筛找相似 → 找到候选
  → LLM 两级去重判定（词表按 §6.0 重映射）：
      candidate 级：skip / create / none
      item 级：merge（并入现有经验）/ delete（= 旧经验 status=superseded）
  → 写或更新 brains/{id}/experiences/
  → memory_diff 审计（可回滚）
```

**关键**：决定"哪些信息进经验库"由 LLM 决策 + 审计，不做无脑 append；**OpenViking 的 delete ≠ 删源 md**。

### 6.5 分层生成（OpenViking L0/L1/L2）

- 文件写入后异步生成 `.abstract`（L0，~100 token）与 `.overview`（L1，~1-2k token）——存 frontmatter 或同级隐藏文件；
- 目录级也有 L0/L1（自底向上聚合）；
- 检索默认只注入 L0/L1，L2 按需读取。

### 6.6 零 LLM 建图（GBrain）

```
extractEntityRefs 四 pass（markdown 链接 / qualified wikilink / wikilink / mention）
先 stripCodeBlocks；masked-ranges 防双发
inferLinkType 正则：decided/produced_by/works_on/references/mentions/belongs_to
```

---

## 7. 检索管线（GBrain 混合检索）

### 7.1 完整链路

```
Query（CLI `query`/`find`/`think` / DSH `memory_query`）
  → 意图分类（零 LLM 正则）：task/experience/person/general
  → **不做** LLM multi-query 扩写（#31）
  → 混合召回（并行）：
      ① 语义检索（向量，embedding 见 §5.6 / D19）
      ② BM25 关键词（PGLite FTS/ngram）
      ③ 图谱遍历（typed edges）
      ④ 实体 boost
      ⑤ 关系查询 arm
  → RRF（k=60）→ rrf' rescale → per-arm floor → 加权融合 → fused floor
  → cosine re-score（embedding 非 off）
  → graph signals
  → hotness **乘法**（α=0.15）
  → 可选 rerank（默认 **off**；失败 local → 不 rerank）
  → 分层标注（P8.2）+ 层 tie-break（EPS=0.002）
  → 排序去重 → 返回
```

### 7.2 融合公式（P9.3 现行）

```
rrf' = rrf * (k+1)     # k=60 不动；rank1 → 1.0
臂贡献：rrf' < per_arm_min（默认 0.70）→ 该臂贡献当 0
final_rel = Σ w * rrf' + w_title*title + w_entity*entity
          # 权重见 P3.1 冻结表；sigmoid / 臂内 min-max **不做**
丢弃 final_rel < fused_min（默认 0.05）
rel = (1-λ)*final_rel + λ*max(0, cosine)   # λ=cosine_lambda 默认 0.3；embedding=off 则跳过
score = rel * (1 + α * hotness)            # α 默认 0.15；**禁止**加法 0.45
```

> **废止** 原文 `0.55×rrf(keyword)+0.30×rrf(graph)+0.15×hotness` 与 Mem0 sigmoid。hotness **不是** FusionWeights 内加项，是 post-fusion 乘子（P3.1 + P9.3）。

> 语义通道不可用时仍走 BM25+图；`--explain` 带 `fusion.rescale` / `floor` / `hotness.alpha`。observer 的 latency / evidence 分布与 `query_plan` **未做**（下期）。

### 7.3 关系查询 arm（GBrain relational recall）

```
parseRelationalQuery（确定性零 LLM）
  → 识别 seed + 动词 → 图遍历（depth≤2）→ hydrate 结果
fail-open：解析失败返回空 arm，不破坏热路径
```

### 7.4 graph signals 调权（GBrain）

| 信号 | 乘子 | 触发条件 |
|---|---|---|
| Adjacency within top-K | ~1.05× | ≥2 个 top-K 节点链入 = hub |
| Cross-source adjacency | ~1.10× | ≥2 个不同 source 链入 = 跨源 hub |
| Session diversification | ~0.95× | 同 source 前缀多个 top-K 节点只留最高分（MMR-lite） |

fail-open：任一信号不可用即跳过，不破坏热路径。

### 7.5 命名实体检索层（GBrain）

- **Per-page max-pool**：LIMIT 前按 path 折叠为每路径最佳 chunk，避免一个页面的弱 chunk 挤掉别页；
- **Title-phrase boost**：查询命中标题连续 token 串时 boost；
- **Alias hop**：`aliases:` frontmatter 投影进索引，查询时桥接零表面重合的同义词；
- **NER 类型链接**：`CEO of Acme` → 自动建 typed link（schema pack 提供正则）。

### 7.6 查询缓存与防污染（GBrain）

- `search_cache` 带 `knobs_hash`（search mode + 权重 + intent 参数折叠进 key），防跨配置污染；
- 升级/改配置后旧行不可达，一次性 miss spike 可接受。

### 7.7 检索可观测（Mem0 explain + OpenViking 轨迹）

- 每结果可带 evidence 通道名 + 部分 `score_details`（`--explain`）；
- `observer` 看总量/零结果/均分；**latency / evidence 分布 / `query_plan` / `searched_directories` 未做**（下期 #22+#32）。

---

## 8. 维护循环（dream cycle）

### 8.1 现行五段（P3.2 v1，**明确不扩九段**）

```
memory dream [--fix] [--phases 1,2,3,4,5]：
  ① lint                 frontmatter/路径/pack 校验
  ② sync                 文件 → 索引增量同步
  ③ distill_pending      未蒸馏源跑 refine（受 kill_switch）
  ④ contradictions       同 path 冲突 facts 启发式 → contradictions.md（不删 facts）
  ⑤ orphans              显式临时 note 且无引用 → status=archived
```

> 原 GBrain 九段（backlinks / synthesize / extract / patterns / recompute_weight / embed --stale）**裁剪**。Iron Law back-link 走写后 enrich（P9.7），不进 dream。无夜间 cron 守护进程。

> dream 默认路径属 **L2**（D17）：只软归档与标记，**无人工确认不做硬删除 / entity merge**。

### 8.2 关键维护规则

- **Iron Law back-link**（P9.7）：写后 enrich 给实体页补反向链；facts 正文可带 `[Source: ...]`。fail-open；**不**在 dream 里跑。
- **不变量**：文件权威永远成立——维护循环只改文件/重建索引，**绝不只改索引**（否则重建丢数据）。
- **幂等**：内容 hash 未变跳过；崩溃恢复以**磁盘文件**为准，重启后索引与文件对齐（`rebuild-index` 兜底）；git 仅恢复「已 flush」的历史版本。

### 8.3 矛盾检测（现行 = 启发式；LLM 三分类未做）

- **快路径（L0）**：候选事实 cosine ≥ 0.95 → 跳过写入（P5.1），零 LLM；
- **dream ④**：同 path 冲突 facts 启发式写入 `contradictions.md`，facts 并存、不自动删；
- cosine + LLM 三分类（duplicate / supersede / independent）属 **下期未锁范围**，禁止按本文原 C 方案直接开工。

---

## 9. 结晶层（MemOS Reflect2Evolve 简化，D5）

### 9.1 流程

```
经验成熟判定：eta_score ≥ 阈值 且 support（被复用次数）≥ 2 且无反例
  → 提炼：把多条同 trigger 经验 LLM 归纳为通用 procedure
  → 状态机：candidate → active → archived（**升格须人手** `skill activate`，不自动 active）
      candidate：初版，待验证
      active：真实任务验证成功
      archived：被反例否定 / 不再适用
  → 反例收集：`skill outcome` / 插件 `memory_skill_outcome` 回写 eta/support（P9.6）
  → 打包：生成 SKILL.md
  → 注册进 `brains/{id}/skills/`（shared/ 需显式授权）
```

### 9.2 产出物：SKILL.md

```markdown
---
name: payment-timeout-fix
trigger: "支付网关超时/重试相关任务"
eta: 0.8
support: 3
status: active
source_trajectory_ids: [task#101, task#202]
counter_examples: []
---
## Procedure
1. 检查重试策略配置（指数退避→固定次数）
2. 确认超时阈值（2s→5s 需业务确认）
3. 验证 500 错误码场景
## Boundary
适用于支付类同步调用；不适用于异步队列场景
## Verification
跑 `make test-payment-timeout` 验证
```

### 9.3 与 df-app 技能体系对接（适配器能力）

- df-memory 的 SKILL.md 与 df-app 现有 skill 规范**同构**（SKILL.md + scripts）；
- df-app 适配器把结晶技能同步进其 mcphub 分发通道（可选插件）。

---

## 10. 注入机制（混合式，D6）

```
任务启动（DSH session-start，被动）：
  注入该 brain **top-3 active 经验**（失败 fail-open）；**不**注 L0、**不**自动注 skill
运行时（agent 主动）：
  DSH `memory_query` / `memory_read` / `memory_skill`（list/find/read/inject）
  新情况 → `memory_remember`（默认入队）
skill **不混**默认 query（P8.3）；不从会话直接抽 SKILL.md
```

### 10.1 agent 适配器（现行仅 DSH）

> 原表（Claude Code / Codex / OpenCode / OpenClaw）**明确不做**。Cursor hook + `/`（P6.5）**不做**。

| Agent | 接入方式 | 注入点 |
|---|---|---|
| **DeepSeek Harness** | 并列仓 `dsh-df-memory` Cordis 插件（P4.2 A + P8.1） | pre-step `next(messages)` 优先；session-start 注经验；挂钩收 user/assistant 进 inbox；达窗 `deferCompile` 入队 |
| **CLI** | `memory remember` / `ingest --adapter session` | 人手/脚本；`--buffer` 默认同步攒窗 |

**通用约定**：DSH 做 (1) 启动注经验；(2) 会话挂钩归档 inbox 并异步 compile。工具面与 CLI 共用 `@lhdrc/core`，**不经 MCP**。

---

## 11. 多租户与权限（单机多租户，D8）

> 单机部署但**多租户是目标**：一个实例服务多个 brain（团队），brain 之间隔离，brain 内 source 隔离。

### 11.1 三级隔离模型

| 层 | 单位 | 隔离 |
|---|---|---|
| **实例** | 单机进程 | 全部 brain 共用 |
| **brain** | 团队/项目 | 完全隔离（目录 + 权限 + 索引 scope） |
| **source** | 任务/session/issue | brain 内隔离（source scope 强制） |

### 11.2 权限模型

| 角色 | 范围 | 能力 |
|---|---|---|
| `owner` | brain | 全部（含删除/权限管理） |
| `admin` | 实例 | 全部 brain + 系统配置 |
| `member` | brain | 读写该 brain 的 source（受 source scope 约束） |
| `reader` | brain | 只读 |

- **身份**：`memory.yml` + CLI `--brain` / 插件 per-call `brain`（P8.5）；REST API token **未做**（无 MCP/REST）；
- **source 访问**：读侧 op 强制 `sourceScope(ctx)`（§4.2）；
- **agent 身份**：插件请求可带 `agent_id`（P5.5）；
- **fail-closed**：未显式授权即拒绝。

### 11.3 权限测试

- **fuzz 隔离**：跨 brain / 跨 source 读路径断言零泄漏（`bun run test:isolation`）；无 admin HTTP API。
- 该测试进 CI（§17），每次权限改动必跑。

## 12. 访问面（现行）

> **P4.1 MCP / REST 明确不做。** agent 通道是 DSH 插件 + CLI。

### 12.1 DSH 插件工具（agent 通道，非 MCP）

| 工具 | 功能 |
|---|---|
| `memory_query` / `memory_read` | 检索 / 读节点（分层标注 P8.2） |
| `memory_remember` / `memory_job` | 写入入队 / 查任务 |
| `memory_skill` list/find/read/inject | 按需技能，不混默认 query |
| `memory_skill_outcome` | 回写 eta/support，不自动 activate |
| per-call `brain` | P8.5 隔离 |

### 12.2 CLI（人/脚本；现行命令面）

`init` · `capture` · `import` · `ingest` · `remember` · `query` · `find` · `think` · `read` · `tree` · `forget` · `entity` · `brain` · `agent` · `schema` · `refine` · `revert` · `dream` · `skill` · `layers` · `graph-query` · `events` · `changes` · `observer` · `inbox` · `job` · `trend` · `rebuild-index` · `sync` · `eval`

`capture` / `remember` / `ingest session` 默认入队；`--wait` 同步。

### 12.3 REST

**未实现。** 无 `GET /health`、无 `POST /api/memory`、无 `memory serve`。

---

## 13. 技术栈（交付锁定）

### 方案 A：TypeScript / Bun（**唯一交付**）

| 项 | 选型 |
|---|---|
| 运行时 | Bun（workspaces；CLI bin `memory`） |
| 权威存储 | md 文件 + frontmatter（文件权威）；git 可选批量账本（D18） |
| 检索索引 | **PGLite**（默认）→ PostgresEngine（P5.7；真 pgvector HNSW **下期**） |
| 图谱/检索 | 自研（RRF rescale / hybrid / graph，P9.3） |
| agent 面 | DSH 插件（并列仓）；**无**官方 MCP SDK 包 |
| CLI | `packages/cli` |
| 发布 | npm 包名 `@lhdrc/core`；**尚未**做 `memory upgrade` / 公开 npm publish 关账 |

**理由**：agent 生态母语；PGLite 零配置。

### 方案 B：Java 17

**明确不做**（原备选表删除）。下文接口契约仅为 TS 实现对照，不承诺 Java 移植。

---

## 13.1 接口契约（核心抽象，TS）

> 与代码同语义；Java 不再维护。生产 LLM **必须**走 `complete()`（P6.1 / P7.1）。

```ts
interface LLMProvider {
  readonly id: string;
  complete(req: CompleteRequest): Promise<CompleteResult>;
  judgeDistill(existing: string[], candidate: string): Promise<DistillDecision>;
  generateAbstract(content: string): Promise<string>;
  generateOverview(children: string[]): Promise<string>;
  refineExperience(ctx: ExperienceContext): Promise<ExperienceResult>;
  extractFacts?(body: string, meta: FactExtractMeta): Promise<ExtractFact[]>;
}

interface EmbeddingProvider {
  readonly id: string;
  readonly dims: number;
  embed(texts: string[]): Promise<number[][]>;
}

interface WriteValidator {
  validate(req: CreateNodeRequest): Promise<ValidationResult>;
}

interface EntityRegistry {
  resolve(alias: string): Promise<Entity>;
  merge(entityIds: string[]): Promise<void>;
  linkFacts(entityId: string, factIds: string[]): Promise<void>;
}

interface EventLedger {
  append(event: MemoryEvent): Promise<void>;
  query(source: string, from?: Date): Promise<MemoryEvent[]>;
}

interface AccessControl {
  authorize(req: AuthedRequest): Promise<AuthContext>;
}
```

`MemoryNodeService` / `MemoryRetrieveService` / `ExperienceService` / `SkillService` 仍是心智模型；现行入口是 core 函数 + CLI/插件，不是独立 HTTP 服务。Skill `onSkillOutcome` **不**自动 `activate`。

---

## 14. 配置与成本控制（GBrain D12）

| 项 | 现行 |
|---|---|
| search mode | conservative / balanced（默认）/ tokenmax；tokenmax **不做 LLM 扩写**；rerank 默认 `off` |
| cost cap | `.dfmemory/costs.jsonl` 记账；超 cap 跳过该类 LLM |
| kill-switch | `llm.kill_switch.{distill,abstract,extract,compile}` |
| 降级链 | LLM 不可用 → `E_DISABLED` 或启发式；语义臂无 Key → 哈希 fail-open；BM25+图始终可跑 |
| 配置 | 仓根 `memory.yml` + env（Key 名由 `openai_api_key_env` 指向）；不绑平台配置中心 |

---

## 15. 开源治理

- **许可证**：MIT（D8）；
- **仓库结构（现行）**：
  ```
  /packages/core              # @lhdrc/core
  /packages/cli               # bin: memory
  /packages/adapters/*        # ingest-generic-jsonl / ingest-df-app / ingest-session
  /evals                      # mini / distill / report / locomo publish（P10.1）
  /specs                      # 可执行 Spec（权威实现切片）
  AGENTS.md                   # agent 导航
  ```
  **无** `packages/mcp`、**无** `examples/` Claude/Codex 模板。DSH 插件在并列仓 `dsh-df-memory/`。
- **文档**：AGENTS.md + `specs/`；llms.txt / CONTRIBUTING / `memory upgrade` **下期**（依赖 npm publish）。
- **评测**：P5.6 hermetic 门禁；对外 LoCoMo 见 P10.1。LongMemEval **不做**。
- **贡献**：`bun test` / `test:isolation` / `typecheck`；检索改动必跑 `eval:mini`。

---

## 17. 测试策略

| 层 | 内容 |
|---|---|
| 单元 | 意图分类/RRF/归一化/正则建图/merge_op/distill 判定 100%；LLM mock |
| 集成 | 真实 PGLite：写文件→索引同步→检索；batch flush 后可 `git log`；蒸馏→结晶→SKILL.md |
| 索引一致性 | 写文件→同步索引→查询；`rebuild-index` 后与文件权威一致（facts 不丢） |
| 检索质量回归 | 12 条手挑查询：top-1≥80%、recall@10≥85%（落线必跑） |
| 隔离 fuzz | 跨 source/brain 读全路径零泄漏 |
| 蒸馏有效性 | 自建 mini-bench：历史任务"有经验 vs 无经验"成功率对比（OpenViking 思路） |
| 崩溃恢复 | 写入中途崩溃 → 重启后以磁盘文件为准对齐索引；未 flush 的 git 历史可落后于工作区 |

### 17.1 评测体系（P5.6 + P10.1）

> hermetic：[`P5.6-evals.md`](../specs/五期/P5.6-evals.md) — `eval:mini` / `eval:distill` / `eval:report`；LoCoMo **`--fixture` 子串口径不得对外发数**。  
> 发数：[`P10.1-locomo-publish.md`](../specs/十期/P10.1-locomo-publish.md) — compile → hybridQuery → answerer → J-score；排除 category 5；蒸馏关。LongMemEval / HaluMem **不做**。公开数字走 CLI `evals/`，**不用 DSH 刷分**。

---

## 18. 落地路线图（实现进度）

> **分期原则**：D1/D2/D13/D17/D18 遵守。Java 不进交付。**P4.1 MCP/REST 明确不做**（不再是「后置待做」）。当前主线：**十期 P10.1**（LoCoMo 发数）。差距见 [`TODO.md`](../TODO.md)。

### 18.1 决策锁定（修订）

| 项 | MVP–三期 | 四期（修订） | 五–九期 |
|---|---|---|---|
| 产品面 | CLI | **DSH 插件**（P4.2）；MCP/REST **不做** | 会话摄入、异步写、检索量纲 |
| 运行面 | 单 brain → 多 brain | per-call `brain`（P8.5） | 同左 |
| 成本面 | 零 LLM 默认可跑 | — | kill-switch + costs.jsonl |
| 检索面 | BM25 → RRF → 图谱 | 分层标注 P8.2 | P9.3 rescale / cosine / hotness 乘法 |
| P0 语义 | D1/D2/D13/D17/D18 | D7 改为 CLI+DSH | D19 embedding 三档 |

### 18.2–18.6

MVP / 二期 / 三期 / 五期 / 六期：**done**（细节仍见原文各节 Spec）。人手 `capture` 仍可零 LLM；会话路径必须 `complete()`。

### 18.7 七期（P7.1–P7.5）— **done**

详见 [`specs/七期/`](../specs/七期/)。`complete()` 覆盖 distill/extract/abstract；滑动窗口；compile 建 entity；`inbox retry` / revert。

### 18.8 四期 — **P4.1 不做**；P4.2 A in_progress

| 里程碑 | 内容 | 状态 |
|---|---|---|
| **P4.1 MCP/REST** | 原「补充期」 | **明确不做** |
| **P4.2 A** | DSH 插件化：core Node 兼容 + 三工具 | 本仓测例绿；**未 npm publish** |
| **P4.2 B** | 会话挂钩 + 异步 remember | **迁 P8.1，done** |

### 18.9 八期（P8.1–P8.5）— **done**（2026-08-17 真机关账）

详见 [`specs/八期/`](../specs/八期/)。挂钩 inbox、分层标注、skill 按需注入、抽取粒度、per-call brain。P81-17 真机 `next` 当步生效已验证。

### 18.10 九期（P9.1–P9.9）— **done**

详见 [`specs/九期/`](../specs/九期/)。content_hash 白名单、embedding 三档、融合量纲、source 7 层、trend、skill outcome、Iron Law、写路径默认异步、蒸馏 `merge_op`。

### 18.11 十期（P10.1）— **in_progress**

详见 [`specs/十期/`](../specs/十期/)。G0 runner + P101 测例已交；全量 `locomo10.json` 发数待 G1–G6。

---

## 19. Spec 索引

> **可执行详规优先于本文未修订段落。** 入口：[`specs/README.md`](../specs/README.md)。

### 19.1 MVP Specs（**done**）

| Spec | 内容 | 详规 |
|---|---|---|
| M1 | 仓库骨架 + 文件权威 + Entity merge | [`specs/mvp/M1-repo-file-authority.md`](../specs/mvp/M1-repo-file-authority.md) |
| M2 | 单写者队列 + WRITE_FORMAT | [`specs/mvp/M2-write-cli.md`](../specs/mvp/M2-write-cli.md) |
| M3 | PGLite + BM25 + rebuild-index | [`specs/mvp/M3-index-query.md`](../specs/mvp/M3-index-query.md) |

### 19.2–19.6 二期至七期（**done**）

详规：[`二期/`](../specs/二期/) · [`三期/`](../specs/三期/) · [`五期/`](../specs/五期/) · [`六期/`](../specs/六期/) · [`七期/`](../specs/七期/)。P6.5 Cursor 模板 **不做**。

### 19.7 四期 Specs

| Spec | 内容 | 状态 |
|---|---|---|
| P4.1 | MCP + REST + Claude Code | **不做** |
| P4.2 | DSH 插件化 | A **in_progress**；B 迁八期 **done** |

### 19.8 八期 Specs

见 [`specs/八期/`](../specs/八期/)（P8.1–P8.5 **done**）。

### 19.9 九期 Specs

见 [`specs/九期/`](../specs/九期/)（P9.1–P9.9 **done**）。

### 19.10 十期 Specs

| Spec | 内容 | 状态 |
|---|---|---|
| P10.1 | LoCoMo 发数协议 + publish runner | **in_progress** |

---

## 20. 风险与开放问题

| 风险 | 缓解/决策 |
|---|---|
| 单机规模上限 | PostgresEngine 可换；真 pgvector HNSW **下期 #9**；不承诺分布式 |
| 蒸馏有效性 | `eval:distill`；L1 可 revert；L0 仍 ADD-only |
| 结晶把坏模式固化 | outcome 回写；**不自动** activate |
| PGLite 中文检索 | P2.1a 门禁；语义臂 openai / 哈希 fail-open |
| 单写者队列跨进程 | 文件锁；CLI 与插件共用 JobRunner（`.dfmemory/jobs/`） |
| git 提交频率（**已关闭 → D18**） | 默认 batch |
| 实体合并误判 | `--confirm`；redirect + event；禁止只改索引 |
| 多租户权限泄漏 | fail-closed + 路径强制 + `test:isolation`；git 历史同仓可见（非密码学隔离） |
| agent 适配器覆盖不全 | **已关**：只做 DSH；MCP 统一通道废止 |
| 双技术栈 | **已关**：仅 TS/Bun |
| 评测发数口径 | fixture ≠ 公开发数；P10.1 J-score；LoCoMo 抽取 prompt 与产品 coding-session 合同不对齐（已知，不改产品 prompt） |
| 写语义冲突（已关闭） | D17 |
| embedding 默认联网 | D19：init openai；CI 必须 local/mock |
| 仍开放（下期，见 TODO） | 图谱扩词表、矛盾 LLM 分类、observer 轨迹、L0 memory_diff、敏感字段 mask、npm publish / upgrade |

---

*本文档 2026-08-18 已按九期实现与明确裁剪修订 ADR（D7/D10/D16/D19）及矛盾章节。工程师实现请跟 [`specs/`](../specs/)；差距只记 [`TODO.md`](../TODO.md)，不要把本文未交付愿景当接口。*
